import { Name } from "../app/(routes)/(home)/_components/faq/faq-question"

export interface Occasion {
    id?: string
    name?: Name
    image?: Image
    createdAt?: string
    updatedAt?: string
    deletedAt?: string
}